# Contao example bundle

Contao example bundle as template for new bundles.

## Install composer packages
    docker run -it --rm \
    -v ".:/var/www/html" \
    laemmi/php-fpm:8.1 \
    composer install